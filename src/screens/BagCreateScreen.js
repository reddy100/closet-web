import React, {useContext} from 'react';
import {View, StyleSheet, Text} from 'react-native';

const BagCreateScreeen = () => {
    return <>
        <Text> BagCreateScreeen</Text>
    </>
}

const styles = StyleSheet.create({
});

export default BagCreateScreeen;